{
  "name": "site-diary",
  "private": true,
  "version": "1.0.0",
  "description": "Site Diary - daily diary app for a site team",
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--no-warnings concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
